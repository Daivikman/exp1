Projects & Labs — Experiments Collection

This repository README contains Experiments 1–6.
Each experiment is presented in a GitHub README-friendly format with three clearly separated parts:

Files — filenames you should create.

Commands — terminal/CLI commands to run (one-per-line blocks).

Code / File contents — exact contents to put into each file (code blocks).

Table of contents

Experiment 1 — Initialize Git & push to GitHub

Experiment 2 — Simple Docker static site (nginx)

Experiment 3 — Flask app in Docker

Experiment 4 — Jenkins static-site pipeline (docker-compose + Jenkins pipeline)

Experiment 5 — Python CI + Docker + GitHub Actions

Experiment 6 — Minikube / Kubernetes local lab

Experiment 1 — Initialize Git & push to GitHub
Files

README.md (optional — this file)

(your project files — e.g., Flutter project files)

Commands
# Initialize git repo
git init

# (optional) create README
echo "# flutterexperiments" >> README.md

# Stage all files
git add .

# Commit changes
git commit -m "first commit"

# Set main branch name
git branch -M main

# Add GitHub remote (replace URL if needed)
git remote add origin https://github.com/deepakurk22cs1081/flutterexperiments.git

# Push to GitHub
git push -u origin main

Notes / Code

No additional code files provided here — use the commands above in your project root.

Experiment 2 — Simple Docker static site (nginx)
Files

index.html

Dockerfile

Commands
# create folder and enter it (optional)
mkdir docker-demo
cd docker-demo

# Build the docker image (tag: myweb:v1)
docker build -t myweb:v1 .

# Run the container mapping port 8080 to container's 80
docker run -d -p 8080:80 myweb:v1

# Open in browser:
# http://localhost:8080

Code / File contents

index.html

<h1>Hello from Docker!</h1>


Dockerfile

FROM nginx
COPY index.html /usr/share/nginx/html/index.html

Experiment 3 — Flask app in Docker
Files

app.py

requirements.txt

Dockerfile

Commands
# create project folder (optional)
mkdir flask-docker-app
cd flask-docker-app

# build docker image (tag: flask-docker-app)
docker build -t flask-docker-app .

# run container (map host port 5000 to container 5000)
docker run -p 5000:5000 flask-docker-app

# if 5000 is occupied, use alternative host port (e.g., 5050):
docker run -p 5050:5000 flask-docker-app

# Open in browser:
# http://localhost:5000   # or http://localhost:5050 if using alternate port

Code / File contents

app.py

from flask import Flask, render_template_string
app = Flask(__name__)

HTML_TEMPLATE = """
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Flask Docker App</title>
<style>
body {
  background: linear-gradient(to right, #00c6ff, #0072ff);
  color: white;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  text-align: center;
  padding-top: 100px;
}
h1 { font-size: 3em; margin-bottom: 0.2em; }
p { font-size: 1.5em; }
.card {
  background: rgba(255, 255, 255, 0.1);
  padding: 2em;
  border-radius: 20px;
  box-shadow: 0 0 20px rgba(0,0,0,0.3);
  width: 50%;
  margin: auto;
}
</style>
</head>
<body>
<div class="card">
  <h1>🚀 Welcome to Flask + Docker!</h1>
  <p>This is a vibrant web page running inside a Docker container.</p>
  <p>Change the code → Rebuild → Restart Docker to see updates!</p>
</div>
</body>
</html>
"""

@app.route('/')
def home():
    return render_template_string(HTML_TEMPLATE)

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)


requirements.txt

Flask==2.2.5


Dockerfile

FROM python:3.9-slim
WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
EXPOSE 5000
CMD ["python", "app.py"]

Experiment 4 — Jenkins static-site pipeline (docker-compose + Jenkins pipeline)
Files

docker-compose.yml

(Jenkins pipeline script — place inside Jenkins Pipeline job as "Pipeline script")

(optional) site/index.html and site/assets/style.css will be generated by the pipeline

Commands
# create folder and enter it
mkdir ~/jenkins-static-site && cd ~/jenkins-static-site

# start Jenkins using docker-compose (in this folder)
docker compose up -d

# to get initial admin password from Jenkins container
docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword

# open Jenkins in browser:
# http://localhost:8080

Code / File contents

docker-compose.yml

version: '3.8'
services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  jenkins_home:
    driver: local


Jenkins Pipeline script (paste into the job Pipeline > Definition: Pipeline script)

pipeline {
  agent any
  stages {
    stage('Generate site') {
      steps {
        script {
          sh 'rm -rf site || true'
          sh 'mkdir -p site/assets'
          writeFile file: 'site/index.html', text: """
<!doctype html>
<html lang=\"en\">
<head>
<meta charset=\"utf-8\"/>
<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"/>
<title>Karunya University — Simple Site</title>
<link rel=\"stylesheet\" href=\"assets/style.css\"/>
</head>
<body>
<header class=\"site-header\">
  <div class=\"container\">
    <h1>Karunya University</h1>
    <p class=\"tagline\">Values — Knowledge — Service</p>
  </div>
</header>
<main class=\"container\">
  <section class=\"card\">
    <h2>About Karunya University</h2>
    <p>Karunya Institute of Technology and Sciences is a leading centre for higher education in India.</p>
  </section>
</main>
<footer class=\"site-footer container\">
  <p>&copy; ${new Date().format('yyyy')} Karunya University — Generated by Jenkins</p>
</footer>
</body>
</html>
"""
          writeFile file: 'site/assets/style.css', text: """
body { font-family: Arial, sans-serif; margin:0; }
.container { max-width:800px; margin:0 auto; padding:20px; }
.site-header { background:#0b3d91; color:#fff; padding:20px; }
.tagline { font-size:14px; opacity:0.8; }
.card { background:#fff; border:1px solid #ddd; padding:15px; margin: 15px 0; border-radius:6px; }
.site-footer { text-align:center; font-size:12px; color:#555; margin-top:20px; }
"""
        }
      }
    }
    stage('Publish site') {
      steps {
        publishHTML ([
          allowMissing: false,
          alwaysLinkToLastBuild: true,
          keepAll: true,
          reportDir: 'site',
          reportFiles: 'index.html',
          reportName: 'Karunya University - Simple Site'
        ])
      }
    }
  }
  post {
    always {
      archiveArtifacts artifacts: 'site/**', fingerprint: true
    }
  }
}

How to run & view

Start Jenkins via Docker Compose (see commands above).

Open Jenkins (http://localhost:8080), complete initial setup, install suggested plugins, create admin user.

Install HTML Publisher Plugin (Manage Jenkins → Plugins → Available → search HTML Publisher).

Create a Pipeline job named karunya-site-pipeline and paste the pipeline script above.

Click Build Now → open the build → click Karunya University - Simple Site to view the generated static site.

Experiment 5 — Python CI + Docker + GitHub Actions
Files

app.py

tests/test_app.py

requirements.txt

Dockerfile

.gitignore

.github/workflows/ci-dockerhub.yml

Commands
# create project dir and enter
mkdir python-ci-docker-lab
cd python-ci-docker-lab

# run locally (optional)
python app.py
pytest -q

# build local docker image
docker build -t yourname/python-ci-lab:local .

# run docker image locally
docker run --rm yourname/python-ci-lab:local

# initialize git and push to GitHub (replace remote URL)
git init
git add .
git commit -m "init lab"
git branch -M main
git remote add origin https://github.com/<your-username>/python-ci-docker-lab.git
git push -u origin main

# add GitHub secrets (in repo settings):
# DOCKERHUB_USERNAME
# DOCKERHUB_TOKEN

Code / File contents

app.py

def add(a, b):
    return a + b

if __name__ == "__main__":
    print("Hello from Python CI Lab!")
    print("2 + 3 =", add(2, 3))


tests/test_app.py

from app import add

def test_add():
    assert add(2, 3) == 5
    assert add(-1, 1) == 0


requirements.txt

pytest==8.3.2


Dockerfile

FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "app.py"]


.gitignore

__pycache__/
.venv/
.pytest_cache/
.DS_Store
*.pyc


.github/workflows/ci-dockerhub.yml

name: ci-dockerhub
on:
  push:
    branches: [ "main" ]
    tags: [ "*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest -q

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/python-ci-lab
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-,format=short
            type=ref,event=tag

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64

Notes

Add Docker Hub secrets in repository Settings → Secrets.

After push to main, GitHub Actions will run tests, build image, and push to Docker Hub.

Experiment 6 — Minikube / Kubernetes local lab
Files

nginx-pod.yaml (example pod manifest)

Commands
# Verify Docker
docker --version

# Verify kubectl
kubectl version --client

# Start Minikube (driver: docker)
minikube start --driver=docker

# Check cluster state
minikube status
kubectl get nodes

# Create a Pod from YAML
kubectl apply -f nginx-pod.yaml

# Check pods
kubectl get pods

# Describe pod for details
kubectl describe pod nginx-pod

# Port-forward to access pod on localhost:8080 -> container port 80
kubectl port-forward pod/nginx-pod 8080:80

# Open http://localhost:8080 in browser

# When finished, delete the pod
kubectl delete pod nginx-pod

# Create deployment
kubectl create deployment my-nginx --image=nginx

# Check deployments and pods
kubectl get deployments
kubectl get pods -l app=my-nginx

# Scale to 3 replicas
kubectl scale deployment my-nginx --replicas=3
kubectl get pods

# Rolling update (example)
kubectl set image deployment/my-nginx nginx=nginx:1.25
kubectl rollout status deployment/my-nginx

# Rollback if needed
kubectl rollout undo deployment/my-nginx

# Expose deployment as NodePort service
kubectl expose deployment my-nginx --type=NodePort --port=80
kubectl get svc

# Get URL for Minikube service (opens browser if desired)
minikube service my-nginx --url

# Cleanup examples
kubectl delete deployment my-nginx
kubectl delete svc my-nginx
kubectl delete all --all -n default

# Stop & delete Minikube cluster (if desired)
minikube stop
minikube delete

Code / File contents

nginx-pod.yaml

apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80

Notes

Ensure Docker, kubectl, and minikube are installed before starting.

On Windows, you can use Chocolatey for kubectl and minikube installation (as noted in your lab outline).

Final notes

Each experiment above is self-contained — create the listed files and run the listed commands in that experiment's folder.

Use the exact file contents in the "Code / File contents" blocks when creating the files.

If you'd like, I can:

generate a single downloadable README.md file with all content above, or

create separate example repos (one per experiment) and provide ready-to-use Git commands to create them locally.
